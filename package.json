{
  "name": "pi-subagents",
  "version": "0.1.0",
  "description": "Interactive Herdr and Zellij agents with conversation branching for pi",
  "type": "module",
  "license": "MIT",
  "keywords": [
    "pi-package",
    "pi",
    "subagent",
    "herdr",
    "zellij"
  ],
  "scripts": {
    "test": "node --test test/*.test.ts",
    "test:herdr": "node --test test/herdr.live.ts",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.84.1",
    "@types/node": "^24.12.0",
    "typescript": "^5.9.3"
  },
  "pi": {
    "extensions": [
      "./extensions/index.ts"
    ]
  }
}
